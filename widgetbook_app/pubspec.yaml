name: widgetbook_app
description: Widgetbook(animated_scroll_view)

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  animated_scroll_view:
    path: ../

  # widgetbook
  widgetbook_annotation: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # lints
  flutter_lints: ^2.0.0

  # widgetbook
  widgetbook: ^3.0.0
  build_runner: ^2.3.3
  widgetbook_generator: ^3.0.0

flutter:
  uses-material-design: true
